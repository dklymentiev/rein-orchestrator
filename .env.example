# Rein - Environment Configuration
# Copy this file to .env and fill in your API key.
#
# You only need ONE provider key -- pick your preferred LLM:

# Anthropic Claude (recommended)
ANTHROPIC_API_KEY=

# OpenAI GPT
# OPENAI_API_KEY=

# OpenRouter (100+ models)
# OPENROUTER_API_KEY=

# Ollama (local models, no key needed)
# OLLAMA_URL=http://host.docker.internal:11434

# AI Gateway (internal gateway proxy)
# AI_GATEWAY_URL=http://localhost:19850
# BRAIN_API_URL=  # Legacy alias for AI_GATEWAY_URL

# WebSocket port for daemon mode (default: 8765)
# REIN_WS_PORT=8765

# Docker volume mounts (used by docker-compose.yml)
# REIN_WORKSPACE=.              # Mount path for one-off workflow runs
# REIN_AGENTS_DIR=./agents      # Agents directory for daemon mode
